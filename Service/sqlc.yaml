version: "2"
sql:
  - engine: postgresql
    queries: db/query
    schema: migrations
    gen:
      go:
        package: dbgen
        out: internal/platform/database/dbgen
        sql_package: pgx/v5
        emit_json_tags: true
